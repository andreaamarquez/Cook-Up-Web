/*const username = document.getElementById('username')
const password = document.getElementById('password')
const button = document.getElementById('button')

button.addEventListener('click', (e) => {
    e.preventDefault()
    const data = {
        username: username.value,
        password: password.value
    }

    console.log(data)
})*/

/*function cargarMensaje() {
    var xhr = new XMLHttpRequest();
    xhr.onreadystatechange = function() {
        if (xhr.readyState === 4 && xhr.status === 200) {
            document.getElementById("mensaje").textContent = xhr.responseText;
        }
    };
    xhr.open("GET", "../php/login.php", true);
    xhr.send();
}

// Llama a la función para cargar el mensaje al cargar la página
window.onload = cargarMensaje;*/

/*function mensaje()
{
    $.get('../php/login.php', function(inf, estado){
        alert('Información de php: '+inf+'\nEstado:'+estado);
    })
}*/

